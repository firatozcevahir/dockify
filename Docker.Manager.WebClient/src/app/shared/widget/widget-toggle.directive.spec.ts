import { WidgetToggleDirective } from './widget-toggle.directive';

describe('WidgetToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetToggleDirective();
    expect(directive).toBeTruthy();
  });
});
