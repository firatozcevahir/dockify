import { MyEchartsDirective } from './my-echarts.directive';

describe('MyEchartsDirective', () => {
  it('should create an instance', () => {
    const directive = new MyEchartsDirective();
    expect(directive).toBeTruthy();
  });
});
